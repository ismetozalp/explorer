# sample.conf - synthetic config file for Explorer preview testing
# This exercises the syntax-highlighted text preview path.

[server]
host = 127.0.0.1
port = 8080
workers = 4

[logging]
level = info
file = /var/log/sample-app.log

[features]
enable_cache = true
enable_metrics = false
